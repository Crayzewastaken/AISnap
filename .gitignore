# Your own settings. config.example.ini is the one in the repo; it gets
# copied to config.ini on first run, and that copy is yours to keep.
config.ini

# Your to-do list. Yours, not the repo's.
todo.txt

# Compiled output
*.exe

# Debug log
ai-snap.log
test-composer.log
preview-ui.log

# Windows junk
Thumbs.db
desktop.ini
